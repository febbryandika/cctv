// TODO(SPEC 10): ffprobe both RTSP URLs; report codec/bitrate and project retention. Masks the password hash.
// Needs a real camera, so it stays manual and out of CI.
console.log('doctor: not implemented (SPEC 10)')
